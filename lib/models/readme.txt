# models

这个目录放应用的数据模型（Model），用于在界面、服务、存储之间传递结构化数据。

## 这个目录里有什么

- note.dart：笔记实体，包含标题、内容、创建时间、修改时间、标签等字段，并提供 JSON 转换与 copyWith。
- settings.dart：应用设置实体，包含主题、字体、滚动同步、存储路径、AI 配置等字段，并提供 JSON 转换与 copyWith。

## 这个目录是干什么用的

- 统一定义核心数据结构，避免在多个地方重复声明字段。
- 作为服务层与界面层之间的数据契约。
- 支持序列化/反序列化，便于本地保存与配置读取。
